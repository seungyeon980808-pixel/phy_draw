# 시각 QA 체크리스트 (20260701)

작성: 2026-07-01 KST · 대상 버전: v0.36.1 (G섹션 v0.36.3) · 저장 스키마: 0.15

이 문서는 **라벨러·각도호·글꼴 정책·선택영역 내보내기**가 최근/향후 변경으로 깨지는지를
사람이 반복 검증하기 위한 수동 QA 세트다. 코드를 수정하지 않으며, 검증 절차와 기대 결과만 정의한다.

## 사용 방법

1. 로컬 서버 실행 후 앱을 연다 (`docs/qa-fixtures/README.md` 참고).
2. 픽스처 `docs/qa-fixtures/label_text_export_fixture.json`을 **파일 → 열기**로 불러온다.
   (불러오기 흐름이 막히면 `label_text_export_expected.md`의 수동 재현 단계를 따른다.)
3. 아래 표의 각 항목을 순서대로 확인하고 **실제 결과 / Pass·Fail / 비고**를 채운다.
4. 회귀 의심 시 캔버스 화면과 export 결과(PNG·SVG)를 함께 비교한다.

> 권장 브라우저: Chrome 또는 Edge (File System Access 저장 picker 지원).
> 글꼴은 export에 임베드되지 않으므로, 내보낸 기기의 설치 글꼴에 따라 모양이 달라질 수 있다(정상 동작).

---

## A. 라벨러 검증

| ID | 확인 항목 | 기대 결과 | 실제 결과 | Pass/Fail | 비고 |
|----|-----------|-----------|-----------|-----------|------|
| A1 | 단일 행 라벨러 (`fx_labeler_single`, "㉠") | 지시선 끝 + 한 줄 텍스트가 p2 위치에 정상 표시 | | | |
| A2 | 여러 줄 라벨러 (`fx_labeler_multiline`, "도르래/(질량 무시)") | 2줄이 중앙 정렬로 표시, 줄 간격 일정 | | | |
| A3 | 한글+로마숫자+원문자 혼합 (`fx_labeler_mixed`) | Ⅰ Ⅱ Ⅲ Ⅳ / ㉠ ㉡ ㉢ ㉣ ㉤ 모두 깨짐 없이 표시 | | | |
| A4 | 더블클릭 편집 | 라벨러 더블클릭 시 작은 텍스트 편집창이 열림 | | | |
| A5 | Enter 줄바꿈 | 편집창에서 Enter가 줄바꿈으로 입력됨 | | | |
| A6 | Ctrl+Enter 확인 | Ctrl+Enter로 편집 확정·창 닫힘, 텍스트 반영 | | | |
| A7 | leader line이 텍스트와 겹치지 않음 | 지시선이 텍스트 블록 가장자리에서 멈추고 글자 위로 지나가지 않음 | | | |
| A8 | 회전/이동 후 텍스트 위치 | 라벨러를 이동·회전해도 텍스트는 똑바로(upright) 유지, 지시선만 따라감 | | | |
| A9 | SVG export 결과 | export SVG에서 라벨러 텍스트·지시선이 캔버스와 동일 | | | |
| A10 | PNG export 결과 | export PNG에서 라벨러 텍스트·지시선이 캔버스와 동일 | | | |

## B. 각도호 라벨 검증

| ID | 확인 항목 | 기대 결과 | 실제 결과 | Pass/Fail | 비고 |
|----|-----------|-----------|-----------|-----------|------|
| B1 | 기본 θ 표시 (`fx_anglearc_theta`) | 호 + θ 라벨이 호 안쪽 적절 위치에 표시 | | | |
| B2 | 커스텀 라벨 (`fx_anglearc_custom`, "α") | α가 정상 표시 | | | |
| B3 | θ를 다른 기호로 수정 | 더블클릭 → 편집창에서 β, Ⅰ, ㉠ 등으로 변경 가능 | | | |
| B4 | 회전(startAngle) 변경 후 위치 | startAngle/sweepAngle 변경 시 라벨이 호를 따라 정상 위치 | | | |
| B5 | 크기(radius) 변경 후 위치 | radius 변경 시 라벨이 호와 함께 자연스럽게 이동 | | | |
| B6 | SVG export 결과 | export SVG에 호·라벨 포함, 선택/반지름 가이드는 제외 | | | |
| B7 | PNG export 결과 | export PNG에 호·라벨 포함, 가이드 제외 | | | |

## C. 글꼴 정책 검증

| ID | 확인 항목 | 기대 결과 | 실제 결과 | Pass/Fail | 비고 |
|----|-----------|-----------|-----------|-----------|------|
| C1 | 일반 텍스트 (`fx_text_normal`) | 돋움 계열, normal (이탤릭 아님) | | | |
| C2 | 라벨러 텍스트 | 돋움 계열, normal (물리량 이탤릭 아님) | | | |
| C3 | 물리량/수식 라벨 (`fx_text_quantity`, `fx_line_label`, 각도호) | Times New Roman 계열 italic | | | |
| C4 | 오브젝트 일반 라벨 (`fx_text_objectlabel`, `fx_rect_objectlabel` "물체 A") | 신명중명조/명조 계열 normal | | | |
| C5 | labelType=quantity vs label 구분 | quantity는 Times italic, label은 명조 normal로 분기 (단, 사각형 라벨은 G1 참고: 항상 명조 normal) | | | |
| C6 | export 결과가 canvas와 일치 | SVG/PNG의 글꼴 스타일이 화면과 동일(글꼴 미설치 시 폴백은 정상) | | | |

## D. 선택영역 내보내기 검증

| ID | 확인 항목 | 기대 결과 | 실제 결과 | Pass/Fail | 비고 |
|----|-----------|-----------|-----------|-----------|------|
| D1 | 기본 줌, 선택영역 PNG export | 드래그한 영역이 정확히 잘려 PNG로 저장됨 | | | |
| D2 | 줌 인 상태 선택영역 PNG export | 줌 인에서도 선택한 월드 영역이 정확히 잘림 | | | |
| D3 | 줌 아웃 상태 선택영역 PNG export | 줌 아웃에서도 영역 크롭 정확 | | | |
| D4 | pan 이동 후 선택영역 PNG export | pan 후에도 화면→월드 변환이 맞아 영역 정확 | | | |
| D5 | 영역에 라벨러/각도호/텍스트 포함 시 | 포함된 객체가 잘리거나 누락되지 않음 (우상단 클러스터 영역 권장) | | | |
| D6 | 가이드/선택핸들/스냅 표시 제외 | export 결과에 가이드·선택핸들·마퀴·스냅 미리보기·그리드가 들어가지 않음 | | | |
| D7 | 선택영역 SVG export | 위 D1~D6을 SVG에서도 동일하게 확인 | | | |

## E. 회귀 확인

| ID | 확인 항목 | 기대 결과 | 실제 결과 | Pass/Fail | 비고 |
|----|-----------|-----------|-----------|-----------|------|
| E1 | 일반 선 도구 | 선이 정상 생성·렌더 | | | |
| E2 | 직선 라벨 (`fx_line_label`, "ℓ") | 선 위 라벨이 일정한 수직 간격으로 표시 | | | |
| E3 | 사각형 라벨 (`fx_rect_objectlabel`) | 사각형 + 라벨 위치(above) 정상 | | | |
| E4 | 원/타원 라벨 (`fx_ellipse_label`, "도르래") | 타원 + 라벨 위치(below) 정상 (구현된 경우) | | | |
| E5 | 회전된 라벨/오브젝트 (`fx_rect_rotated_quantity`, 25°) | 사각형은 회전, 라벨 글자 F는 upright·명조 normal 유지(이탤릭 아님, G1 참고) | | | |
| E6 | 저장/불러오기 | 픽스처를 저장 후 다시 열어도 모든 객체·속성 보존 | | | |
| E7 | 전체 PNG export | 아트보드 영역 전체가 흰 배경 PNG로 정상 출력 | | | |
| E8 | 전체 SVG export | 아트보드 영역 전체가 투명 배경 SVG로 정상 출력 | | | |

## F. 목업 이미지→객체 가져오기 (v0.36.2 추가)

픽스처: `docs/qa-fixtures/image_to_object_mock_v1.json` · 기대값: `image_to_object_mock_v1_expected.md`
경로: 좌측 패널 **고급 기능 → "이미지 → 객체 (목업)"**.

| ID | 확인 항목 | 기대 결과 | 실제 결과 | Pass/Fail | 비고 |
|----|-----------|-----------|-----------|-----------|------|
| F1 | 목업 JSON 가져오기 | 요약 "가져올 객체 10개 · 건너뛴 객체 3개" 표시 | | | |
| F2 | 지원 객체 변환 | line·arrow(→line)·rect·circle/ellipse·triangle·polyline·text·labeler·anglearc 삽입 | | | |
| F3 | 미지원/오류 객체 | spline·p2누락 line·text누락 3건이 경고와 함께 건너뜀, 앱 크래시 없음 | | | |
| F4 | 삽입 후 편집 | 삽입 객체가 선택·이동·편집 가능, 활성도구 V로 복귀 | | | |
| F5 | 한 번 되돌리기 | Ctrl+Z 1회로 삽입 10개가 한 번에 사라짐(그룹 1스텝) | | | |
| F6 | 잘못된 입력 | version 불일치/깨진 JSON은 경고만 표시, 현재 작업 보존 | | | |
| F7 | 삽입 후 내보내기 | 삽입 객체가 전체/선택영역 PNG·SVG에 정상 포함 | | | |

## G. 글꼴·로마숫자·텍스트 입력 수정 (v0.36.3)

관련 수정: 사각형 라벨 정체 강제 · 텍스트 도구 Enter 줄바꿈 · 로마 숫자 세리프 렌더.
픽스처 없이 앱에서 직접 재현해도 된다(별도 사각형 2개 + 텍스트 도구).

| ID | 확인 항목 | 기대 결과 | 실제 결과 | Pass/Fail | 비고 |
|----|-----------|-----------|-----------|-----------|------|
| G1 | 사각형 라벨 A·B 스타일 일치 | 사각형 2개에 각각 라벨 A, B를 주면 **둘 다 신명중명조 정체(normal)** — B가 이탤릭으로 튀지 않음 | | | |
| G2 | 사각형 라벨 quantity 선택 시에도 정체 | 인스펙터에서 사각형 라벨 종류를 "물리량"으로 바꿔도 **이탤릭이 되지 않고 명조 normal 유지** | | | |
| G3 | 텍스트 도구 Enter 줄바꿈 | T 도구로 입력 중 Enter가 **줄바꿈**으로 들어가고 편집창이 닫히지 않음 | | | |
| G4 | 텍스트 도구 Ctrl+Enter 확정 | Ctrl+Enter(또는 "확인")로 커밋, 2줄 텍스트가 캔버스에 2줄로 렌더 | | | |
| G5 | 로마 숫자 세리프 (텍스트) | "마찰 구간 I", "마찰 구간 II" 입력 시 로마 숫자 I·II가 **세리프(명조) 정체**로 표시 | | | |
| G6 | 유니코드 로마 숫자 세리프 | "Ⅰ", "Ⅱ" 입력 시 세리프 글리프로 표시(민무늬 산세리프 아님) | | | |
| G7 | 라이브 미리보기 일치 | 편집창 미리보기의 로마 숫자 모양이 커밋 후 캔버스와 동일 | | | |
| G8 | export 일치 | G1·G5·G6을 SVG·PNG로 내보내도 화면과 동일(글꼴 미설치 시 serif 폴백은 정상) | | | |
| G9 | 저장/불러오기 유지 | 위 텍스트·사각형 라벨을 저장 후 다시 열어도 스타일·줄바꿈 보존 | | | |
| G10 | 라벨러 회귀 없음 | 라벨러 편집창(Enter 줄바꿈·Ctrl+Enter 확인)이 이전과 동일하게 동작 | | | |

> 검증 메모(코드 감사): 선택영역 내보내기의 화면→월드 변환은 `export-dialog.js`의
> `screenToWorld()`(SVG 네이티브 `getScreenCTM().inverse()`)를 사용하므로 줌/팬/레터박싱을
> 모두 반영한다. coord-debug 오버레이(키 `d`)의 `Δscreen ≈ 0`으로 라운드트립이 확인된다.
> export는 월드 좌표 `bounds`를 viewBox·clip으로 그대로 사용 → 선택 사각형과 일치. (로직 변경 없음)

---

### 판정 기준

- **Pass**: 기대 결과와 화면/export가 일치.
- **Fail**: 위치·글꼴·잘림·누락·겹침 중 하나라도 어긋남 → 비고에 증상과 대상 ID 기록.
- 회귀 발견 시: 어떤 커밋 이후 깨졌는지 `git log --oneline`로 추적하고, 본 체크리스트 ID를 이슈/백로그에 인용한다.
